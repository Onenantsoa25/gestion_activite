<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de bord collaborateur</title>
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <!-- Bootstrap Icons CDN (IMPORTANT pour les icônes du sidebar) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <!-- Chart.js CDN pour les diagrammes -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
    <style>
        /* Styles pour le corps et le padding pour le sidebar */
        body {
            padding-left: 280px; /* Largeur du sidebar */
            background-color: #F5F5F5; /* Nouvelle couleur de fond gris très doux pour le contenu principal */
        }
        /* Pour la responsivité sur mobile, vous pourriez vouloir cacher le sidebar fixe et utiliser un offcanvas */
        @media (max-width: 767.98px) { /* Moins de 768px (taille md de Bootstrap) */
            body {
                padding-left: 0; /* Pas de padding sur mobile si le sidebar est un offcanvas */
            }
            .sidebar-fixed-desktop { /* Cache le sidebar fixe sur mobile */
                display: none !important;
            }
        }

        /* Styles pour les cartes du tableau de bord */
        .card {
            border-radius: 12px; /* Bords plus arrondis */
            border: 1px solid #e0e0e0; /* Bordure subtile */
            /* Ombre plus prononcée mais toujours douce pour un effet "très moderne" et flottant */
            box-shadow: 0 8px 25px rgba(0,0,0,0.1); /* Augmentation du flou et de l'opacité */
        }

        /* Styles pour simuler le graphique en camembert */
        .pie-chart-container {
            position: relative;
            width: 200px; /* Taille du graphique */
            height: 200px;
            border-radius: 50%;
            background: conic-gradient(
                #0d6efd 0% 25%, /* Bleu pour 'en cours' (25%) */
                #dc3545 25% 35%, /* Rouge pour 'à faire' (10%) */
                #198754 35% 100% /* Vert pour 'terminé' (65%) */
            );
            margin: 0 auto; /* Centrer le graphique */
        }

        .pie-chart-legend {
            list-style: none;
            padding: 0;
            margin-top: 1rem;
        }
        .pie-chart-legend li {
            display: flex;
            align-items: center;
            margin-bottom: 0.5rem;
        }
        .pie-chart-legend .color-box {
            width: 15px;
            height: 15px;
            margin-right: 0.5rem;
            border-radius: 3px;
        }
        .color-box.blue { background-color: #0d6efd; }
        .color-box.red { background-color: #dc3545; }
        .color-box.green { background-color: #198754; }

        /* Styles pour la ligne verticale dans "Temps passé" */
        .vertical-divider {
            border-left: 1px solid #dee2e6; /* Couleur de bordure Bootstrap */
            height: 60px; /* Hauteur de la ligne */
            margin: 0 1.5rem;
        }
    </style>
</head>
<body>
    {# Inclusion du Sidebar - Le fichier templates/components/sidebar.html.twig doit exister #}
    <div class="sidebar-fixed-desktop">
        {% include 'components/sidebar.html.twig' %}
    </div>

    <main class="container-fluid py-4">
        <h1 class="mb-4 text-center">Tableau de bord collaborateur</h1>

        <div class="row g-4 mb-4"> {# Première ligne pour les deux premiers éléments #}
            {# Section Tâches #}
            <div class="col-md-6"> {# Prend la moitié de la largeur sur les écrans moyens et plus #}
                <div class="card h-100">
                    <div class="card-body text-center">
                        <h5 class="card-title mb-4">Tâches</h5>
                        <div class="pie-chart-container mb-4"></div>
                        <ul class="pie-chart-legend">
                            <li><span class="color-box blue"></span> En cours</li>
                            <li><span class="color-box red"></span> À faire</li>
                            <li><span class="color-box green"></span> Terminé</li>
                        </ul>
                    </div>
                </div>
            </div>

            {# Section Temps passé #}
            <div class="col-md-6"> {# Prend l'autre moitié de la largeur #}
                <div class="card h-100">
                    <div class="card-body d-flex flex-column justify-content-center align-items-center">
                        <h5 class="card-title mb-4">Temps passé</h5>
                        <div class="d-flex align-items-center">
                            <div class="text-center me-4">
                                <p class="mb-0 text-secondary fw-semibold">Jour</p>
                                <h3 class="fw-bold">48h</h3>
                            </div>
                            <div class="vertical-divider"></div>
                            <div class="text-center ms-4">
                                <p class="mb-0 text-secondary fw-semibold">Semaine</p>
                                <h3 class="fw-bold">54h</h3>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <div class="row g-4"> {# Deuxième ligne pour le dernier élément #}
            {# Section Charge de travail #}
            <div class="col-md-12"> {# Prend toute la largeur sur les écrans moyens et plus #}
                <div class="card h-100">
                    <div class="card-body">
                        <h5 class="card-title mb-4 text-center">Charge de travail</h5>
                        <div style="height: 250px;"> {# Conteneur pour le graphique #}
                            <canvas id="workloadChart"></canvas>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <!-- Bootstrap JS CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const ctx = document.getElementById('workloadChart');

            new Chart(ctx, {
                type: 'bar', // Changé de 'line' à 'bar'
                data: {
                    labels: ['04-08-2025', '05-08-2025', '06-08-2025', '07-08-2025', '08-08-2025'], // Dates sur l'axe X
                    datasets: [{
                        label: 'Nombre de tâches',
                        data: [8, 5, 10, 12, 9], // Nombre de tâches sur l'axe Y
                        backgroundColor: '#0178d9ff', // Couleur des barres (bleu clair)
                        borderColor: '#1065abff', // Bordure des barres
                        borderWidth: 1
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false, // Permet de contrôler la hauteur via CSS
                    plugins: {
                        legend: {
                            display: false // Ne pas afficher la légende du dataset
                        },
                        tooltip: {
                            mode: 'index',
                            intersect: false,
                        }
                    },
                    scales: {
                        x: {
                            title: {
                                display: true,
                                text: 'Date'
                            },
                            grid: {
                                display: false // Masquer les lignes de grille verticales
                            }
                        },
                        y: {
                            title: {
                                display: true,
                                text: 'Nombre de tâches'
                            },
                            beginAtZero: true, // Commencer l'axe Y à zéro
                            ticks: {
                                precision: 0 // Afficher des nombres entiers pour les tâches
                            },
                            grid: {
                                color: '#e0e0e0' // Couleur des lignes de grille horizontales
                            }
                        }
                    }
                }
            });
        });
    </script>
</body>
</html>
