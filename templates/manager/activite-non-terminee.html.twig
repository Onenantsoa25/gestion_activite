<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{% block title %}Toutes les activités non terminées{% endblock %}</title>
  <!-- Bootstrap CSS CDN -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
  <!-- Bootstrap Icons CDN (IMPORTANT pour les icônes du sidebar) -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
  <style>
      /* Styles pour le corps et le padding pour le sidebar */
      body {
          padding-left: 280px; /* Largeur du sidebar */
          background-color: #F5F5F5; /* Couleur de fond gris très doux pour le contenu principal */
      }
      /* Pour la responsivité sur mobile, vous pourriez vouloir cacher le sidebar fixe et utiliser un offcanvas */
      @media (max-width: 767.98px) { /* Moins de 768px (taille md de Bootstrap) */
          body {
              padding-left: 0; /* Pas de padding sur mobile si le sidebar est un offcanvas */
          }
          .sidebar-fixed-desktop { /* Cache le sidebar fixe sur mobile */
              display: none !important;
          }
      }

      /* Styles pour les cartes (si utilisées pour le tableau) */
      .card {
          border-radius: 12px; /* Bords plus arrondis */
          border: 1px solid #e0e0e0; /* Bordure subtile */
          box-shadow: 0 4px 15px rgba(0,0,0,0.08); /* Ombre plus douce et moderne */
      }

      /* Styles pour les en-têtes de tableau */
      .table-header-gray {
          background-color: #e9ecef; /* Couleur grise claire de Bootstrap pour les en-têtes */
          color: #343a40; /* Couleur de texte foncée pour un meilleur contraste */
          vertical-align: middle; /* Alignement vertical au milieu */
          font-weight: 600; /* Un peu plus gras pour les en-têtes */
          padding: 1.2rem 1.5rem; /* Padding augmenté pour les en-têtes */
          border-bottom: 2px solid #dee2e6; /* Bordure plus prononcée sous l'en-tête */
          text-transform: uppercase; /* Texte en majuscules pour un look moderne */
          font-size: 0.9rem; /* Taille de police légèrement plus petite */
      }

      /* Styles pour le tableau avec bordures Bootstrap */
      .table {
          border-collapse: collapse; /* Fusionne les bordures des cellules */
          margin-bottom: 0; /* Supprime la marge par défaut du tableau */
          border-radius: 12px; /* Bords arrondis pour le tableau entier */
          overflow: hidden; /* Cache les coins des bordures internes */
          border: 1px solid #dee2e6; /* Bordure générale du tableau */
      }
      .table th,
      .table td {
          border: 1px solid #dee2e6; /* Bordures pour toutes les cellules */
          padding: 0.8rem 1.2rem; /* Padding réduit pour une hauteur de ligne plus petite */
          vertical-align: middle; /* Alignement vertical par défaut */
      }
      /* Supprime la bordure inférieure de la dernière ligne pour un rendu propre */
      .table tbody tr:last-child {
          border-bottom: none;
      }

      /* Centrer le contenu des cellules des boutons */
      .table td.text-center-buttons {
          text-align: center;
          vertical-align: middle;
          width: 1%; /* Rendre la colonne du bouton compacte */
          white-space: nowrap; /* Empêche le texte du bouton de passer à la ligne */
      }

      /* Nouvelle règle CSS pour la colonne d'activité */
      .table tbody td:first-child {
          word-break: break-word; /* Permet aux mots longs de se couper et de passer à la ligne */
      }

      /* Style pour les boutons d'icône */
      .btn-icon {
          padding: 0.5rem 0.75rem; /* Ajuster le padding pour les icônes */
          font-size: 1rem; /* Taille de l'icône */
          line-height: 1; /* Assurer un bon alignement vertical de l'icône */
      }

      /* Styles pour la colonne Date d'échéance */
      .col-date-echeance {
          width: 150px; /* Largeur fixe pour la date d'échéance */
          white-space: nowrap; /* Empêche la date de passer à la ligne */
      }
  </style>
  {% block stylesheets %}{% endblock %}
</head>
<body>
  {# Inclusion directe du Sidebar #}
  <div class="sidebar-fixed-desktop col-md-2">
      {% include 'components/sidebar-manager.html.twig'  with {'active': 'activites'} %}
  </div>

  <main class="container-fluid py-5 col-md-10">
      <h1 class="mb-5 text-center">Toutes les activités non terminées</h1>
        {# <div class="col-md-3" ></div> #}
      <div class="row justify-content-center ">
          {# Augmentation de la largeur de la colonne pour la carte du tableau #}
          <div class="col-md-11 col-lg-10"> {# Prend plus de largeur sur les écrans moyens et grands #}
              <div class="card h-100">
                  <div class="card-body p-0">
                      <h5 class="card-title mb-3 px-4 pt-4 text-center">Détails des activités</h5>
                      <div class="table-responsive">
                          <table class="table table-hover table-bordered mb-0">
                              <thead>
                                  <tr>
                                      <th scope="col" class="table-header-gray">Activité</th>
                                      <th scope="col" class="table-header-gray col-date-echeance">Date d'échéance</th>
                                      <th scope="col" class="table-header-gray text-center-buttons">Action</th> {# Centrer l'en-tête de la colonne d'action #}
                                  </tr>
                              </thead>
                              <tbody>
                                  {# Exemple d'activités non terminées. En production, ces données viendraient de votre backend. #}
                                  {% for activite in activites %}
                                    <tr>
                                        <td class="align-middle">{{activite.activite}}</td>
                                        <td class="align-middle col-date-echeance">{{ activite.dateEcheance|date('d/m/Y') }}</td>
                                        <td class="text-center-buttons">
                                            <a href="/manager/activite/taches/{{activite.id}}">
                                                <button type="button" class="btn btn-sm btn-primary view-tasks-btn btn-icon" data-activity-id="1" title="Voir les tâches">
                                                    <i class="bi bi-eye"></i>
                                                </button>
                                            </a>
    {% if activite.dateDebut is null %}
        <!-- Bouton "Commencer" en vert -->
        <a href="/manager/activite/commencer/{{activite.id}}">
            <button type="button" class="btn btn-sm" style="background-color: #46b809ff; color: #fff; border: none; margin-left: 0.5rem;" title="Commencer l'activité">
                Commencer
            </button>
        </a>
    {% else %}
        <!-- Badge "Déjà commencée" en gris clair -->
        <span class="badge" style="background-color: #adb5bd; color: #fff; margin-left: 0.5rem;">
            Déjà commencée
        </span>
    {% endif %}
                                        </td>
                                    </tr>                                    
                                  {% endfor %}
                                  {# <tr>
                                      <td class="align-middle">Activité 1</td>
                                      <td class="align-middle col-date-echeance">31-07-2025</td>
                                      <td class="text-center-buttons">
                                          <button type="button" class="btn btn-sm btn-primary view-tasks-btn btn-icon" data-activity-id="1" title="Voir les tâches">
                                              <i class="bi bi-eye"></i>
                                          </button>
                                      </td>
                                  </tr>
                                  <tr>
                                      <td class="align-middle">Activité 2</td>
                                      <td class="align-middle col-date-echeance">05-07-2025</td>
                                      <td class="text-center-buttons">
                                          <button type="button" class="btn btn-sm btn-primary view-tasks-btn btn-icon" data-activity-id="2" title="Voir les tâches">
                                              <i class="bi bi-eye"></i>
                                          </button>
                                      </td>
                                  </tr> #}
                                  {# Ajoutez d'autres lignes de données ici si nécessaire #}
                              </tbody>
                          </table>
                      </div>
                  </div>
              </div>
          </div>
      </div>

      {% block page_content %}{% endblock %}
  </main>

  <!-- Bootstrap JS CDN -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
  {% block javascripts %}{% endblock %}

  <script>
      document.addEventListener('DOMContentLoaded', function() {
          document.querySelectorAll('.view-tasks-btn').forEach(button => {
              button.addEventListener('click', function() {
                  const activityId = this.dataset.activityId;
                  console.log(`Bouton 'Voir les tâches' cliqué pour l'activité ID: ${activityId}`);
                  // Ici, vous intégreriez votre logique pour rediriger vers une page
                  // affichant les tâches spécifiques à cette activité.
                  // Exemple: window.location.href = `/activities/${activityId}/tasks`;
              });
          });
      });
  </script>
</body>
</html>
</merged_code>
