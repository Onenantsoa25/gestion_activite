<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  
  <title>{% block title %}Activités soumises par l'équipe{% endblock %}</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />

  <style>
    body {
      padding-left: 280px;
      background-color: #F5F5F5;
      transition: padding-left 0.3s ease;
    }

    .sidebar-fixed-desktop {
      width: 280px;
      position: fixed;
      top: 0;
      left: 0;
      height: 100vh;
      background: linear-gradient(to bottom, #ffffffdc, #c8dbebff);
      padding: 1rem;
      z-index: 1050;
      overflow-y: auto;
    }

    @media (max-width: 767.98px) {
      body { padding-left: 0; }
      .sidebar-fixed-desktop { display: none; }
    }

    main.container-fluid {
      margin-left: 280px;
      width: calc(100% - 280px);
      padding: 1rem;
      transition: margin-left 0.3s ease, width 0.3s ease;
    }

    @media (max-width: 767.98px) {
      main.container-fluid { margin-left: 0; width: 100%; padding: 1rem 0.5rem; }
    }

    .mobile-menu-bar {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      z-index: 1100;
      background: white;
      padding: 0.75rem 1rem;
      box-shadow: 0 2px 6px rgb(0 0 0 / 0.1);
      align-items: center;
      justify-content: space-between;
    }
    @media (max-width: 767.98px) {
      .mobile-menu-bar { display: flex; }
    }

    /* --- Table et cartes --- */
    .card {
      border-radius: 12px;
      border: 1px solid #e0e0e0;
      box-shadow: 0 4px 15px rgba(0,0,0,0.08);
    }
    .table {
      border-collapse: collapse;
      width: 100%;
      margin-bottom: 0;
    }
    .table thead th {
      color: #555;
      font-weight: 600;
      padding: 0.8rem 1rem;
      border-bottom: 2px solid #e0e0e0;
      text-transform: uppercase;
      font-size: 0.8rem;
      letter-spacing: 0.05em;
      border-right: 1px solid #e0e0e0;
    }
    .table thead th:last-child { border-right: none; }
    .table tbody td {
      padding: 0.8rem 1rem;
      border-bottom: 1px solid #eee;
      border-right: 1px solid #eee;
      color: #333;
    }
    .table tbody td:last-child { border-right: none; }
    .table-hover tbody tr:hover {
      background-color: #f8f9fa;
      cursor: pointer;
    }
    .table tbody tr:last-child td { border-bottom: none; }
    .table td.text-center-buttons {
      text-align: center;
      vertical-align: middle;
      width: 1%;
      white-space: nowrap;
    }

    /* Boutons */
    .btn-action {
      border-radius: 6px;
      padding: 0.4rem 0.6rem;
      font-size: 0.9rem;
      font-weight: 500;
      transition: background-color 0.2s ease, transform 0.1s ease;
    }
    .btn-action:hover { transform: translateY(-1px); }
    .btn-action:active { transform: translateY(0); }
    .btn-view { background-color: #0d6efd; color: white; }
    .btn-view:hover { background-color: #0b5ed7; }
    .btn-delete { background-color: #dc3545; color: white; }
    .btn-delete:hover { background-color: #c82333; }
    .btn-modify { background-color: #ffc107; color: #212529; }
    .btn-modify:hover { background-color: #e0a800; }
  </style>
  {% block stylesheets %}{% endblock %}
</head>
<body>

  <!-- Sidebar fixe (desktop) -->
  <div class="sidebar-fixed-desktop">
    {% include 'components/sidebar-manager.html.twig' with { 'active': 'soumises' } %}
  </div>

  <!-- Menu mobile -->
  <div class="mobile-menu-bar d-md-none">
    <h5 class="mb-0">Menu</h5>
    <button class="btn btn-outline-primary" type="button" data-bs-toggle="offcanvas" data-bs-target="#mobileSidebar">
      <i class="bi bi-list fs-4"></i>
    </button>
  </div>

  <!-- Sidebar mobile -->
  <div class="offcanvas offcanvas-start" tabindex="-1" id="mobileSidebar">
    <div class="offcanvas-header">
      <h5 class="offcanvas-title">Menu</h5>
      <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas"></button>
    </div>
    <div class="offcanvas-body">
      {% include 'components/sidebar-manager.html.twig' with { 'active': 'soumises' } %}
    </div>
  </div>

  <!-- Contenu -->
  <main class="container-fluid py-5">
    <h1 class="mb-5 text-center">Activité soumise par l'équipe</h1>

    <div class="row justify-content-center">
      <div class="col-md-11 col-lg-10">
        <div class="card h-100">
          <div class="card-body p-0">
            <h5 class="card-title mb-3 px-4 pt-4 text-center">Détails des activités soumises</h5>
            <div class="table-responsive">
              <table class="table table-hover mb-0">
                <thead>
                  <tr>
                    <th>Activité</th>
                    <th>Collaborateur</th>
                    <th>Date d'échéance</th>
                    <th>Type</th>
                    <th class="text-center-buttons">Actions</th>
                  </tr>
                </thead>
                <tbody>
                  {% for activite in activites %}
                  <tr data-activity-id="{{activite.id}}">
                    <td>{{activite.activite}}</td>
                    <td>{{activite.utilisateurAuteur.matricule}}</td>
                    <td>{{activite.dateEcheance|date('d/m/Y')}}</td>
                    <td>{{activite.typeActivite.typeActivite}}</td>
                    <td class="text-center-buttons">
                      <div class="d-flex justify-content-center gap-2">
                        <a href="/taches/activite/soumise/{{activite.id}}">
                          <button type="button" class="btn btn-action btn-view" title="Voir les tâches">
                            <i class="bi bi-eye"></i>
                          </button>
                        </a>
                        <a href="/manager/activite/supprimer/{{activite.id}}">
                          <button type="button" class="btn btn-action btn-delete" title="Supprimer">
                            <i class="bi bi-trash"></i>
                          </button>
                        </a>
                        <a href="/manager/modifier/activite/{{activite.id}}">
                          <button type="button" class="btn btn-action btn-modify" title="Modifier">
                            <i class="bi bi-pencil"></i>
                          </button>
                        </a>
                      </div>
                    </td>
                  </tr>
                  {% endfor %}
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>

    {% block page_content %}{% endblock %}
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  {% block javascripts %}{% endblock %}
</body>
</html>
