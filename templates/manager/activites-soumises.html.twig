<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Activités soumises par l'équipe{% endblock %}</title>
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <!-- Bootstrap Icons CDN (IMPORTANT pour les icônes du sidebar) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        /* Styles pour le corps et le padding pour le sidebar */
        body {
            padding-left: 280px; /* Largeur du sidebar */
            background-color: #F5F5F5; /* Couleur de fond gris très doux pour le contenu principal */
        }
        /* Pour la responsivité sur mobile, vous pourriez vouloir cacher le sidebar fixe et utiliser un offcanvas */
        @media (max-width: 767.98px) { /* Moins de 768px (taille md de Bootstrap) */
            body {
                padding-left: 0; /* Pas de padding sur mobile si le sidebar est un offcanvas */
            }
            .sidebar-fixed-desktop { /* Cache le sidebar fixe sur mobile */
                display: none !important;
            }
        }

        /* Styles pour les cartes (si utilisées pour le tableau) */
        .card {
            border-radius: 12px; /* Bords plus arrondis */
            border: 1px solid #e0e0e0; /* Bordure subtile */
            box-shadow: 0 4px 15px rgba(0,0,0,0.08); /* Ombre plus douce et moderne */
        }

        /* STYLES DE TABLEAU MODERNISÉS (type Bootstrap épuré) */
        .table { /* Utilise la classe Bootstrap par défaut */
            border-collapse: collapse; /* Fusionne les bordures des cellules */
            width: 100%;
            margin-bottom: 0;
            overflow: hidden; /* Assure que les coins du tableau sont coupés par le border-radius de la carte */
        }

        .table thead th {
            color: #555; /* Couleur de texte plus douce */
            font-weight: 600;
            padding: 0.8rem 1rem; /* Padding réduit pour l'en-tête */
            text-align: left;
            border-bottom: 2px solid #e0e0e0; /* Bordure plus prononcée sous l'en-tête */
            text-transform: uppercase;
            font-size: 0.8rem; /* Taille de police légèrement plus petite */
            letter-spacing: 0.05em;
            border-right: 1px solid #e0e0e0; /* Bordure verticale subtile */
        }
        .table thead th:last-child {
            border-right: none; /* Pas de bordure à droite pour la dernière colonne d'en-tête */
        }

        .table tbody td {
            padding: 0.8rem 1rem; /* Padding réduit pour les cellules */
            vertical-align: middle;
            border-bottom: 1px solid #eee; /* Bordure très légère entre les lignes */
            border-right: 1px solid #eee; /* Bordure verticale très légère */
            color: #333;
        }
        .table tbody td:last-child {
            border-right: none; /* Pas de bordure à droite pour la dernière colonne de données */
        }

        /* Effet de survol pour les lignes du tableau (fourni par .table-hover de Bootstrap) */
        .table-hover tbody tr:hover {
            background-color: #f8f9fa; /* Fond légèrement grisé au survol */
            cursor: pointer;
        }

        /* Supprime la bordure inférieure de la dernière ligne */
        .table tbody tr:last-child td {
            border-bottom: none;
        }

        /* Centrer le contenu des cellules des boutons */
        .table td.text-center-buttons {
            text-align: center;
            vertical-align: middle;
            width: 1%; /* Rendre la colonne du bouton compacte */
            white-space: nowrap; /* Force le contenu de la cellule à rester sur une seule ligne */
        }

        /* Styles pour les boutons d'action */
        .btn-action {
            border-radius: 6px;
            padding: 0.4rem 0.6rem; /* Padding réduit pour les boutons icônes */
            font-size: 0.9rem; /* Taille de police pour les icônes */
            font-weight: 500;
            transition: background-color 0.2s ease, transform 0.1s ease;
            /* margin: 0 0.15rem; -- Supprimé pour utiliser uniquement le gap du parent */
        }
        .btn-action:hover {
            transform: translateY(-1px);
        }
        .btn-action:active {
            transform: translateY(0);
        }
        .btn-view {
            background-color: #0d6efd; /* Bleu primaire */
            color: white;
        }
        .btn-view:hover {
            background-color: #0b5ed7;
        }
        .btn-delete {
            background-color: #dc3545; /* Rouge danger */
            color: white;
        }
        .btn-delete:hover {
            background-color: #c82333;
        }
        .btn-modify {
            background-color: #ffc107; /* Jaune warning */
            color: #212529; /* Texte foncé pour le contraste */
        }
        .btn-modify:hover {
            background-color: #e0a800;
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    {# Inclusion directe du Sidebar #}
    <div class="sidebar-fixed-desktop">
        {% include 'components/sidebar.html.twig' %}
    </div>

    <main class="container-fluid py-5">
        <h1 class="mb-5 text-center">Activité soumise par l'équipe</h1>

        <div class="row justify-content-center">
            {# Augmentation de la largeur de la colonne pour la carte du tableau #}
            <div class="col-md-11 col-lg-10"> {# Prend plus de largeur sur les écrans moyens et grands #}
                <div class="card h-100">
                    <div class="card-body p-0">
                        <h5 class="card-title mb-3 px-4 pt-4 text-center">Détails des activités soumises</h5>
                        <div class="table-responsive">
                            <table class="table table-hover mb-0"> {# Utilisation des classes Bootstrap par défaut #}
                                <thead>
                                    <tr>
                                        <th>Activité</th>
                                        <th>Collaborateur</th>
                                        <th>Date d'échéance</th>
                                        <th>Temps d'estimation</th>
                                        <th>Priorité</th>
                                        <th class="text-center-buttons">Actions</th> {# Colonne unique pour les actions #}
                                    </tr>
                                </thead>
                                <tbody>
                                    {# Exemple d'activités soumises. En production, ces données viendraient de votre backend. #}
                                    <tr data-activity-id="1">
                                        <td>Activité 1</td>
                                        <td>Collaborateur 1</td>
                                        <td>06-08-2025</td>
                                        <td>2</td>
                                        <td>urgent</td>
                                        <td class="text-center-buttons"> {# Colonne unique pour les boutons d'action #}
                                            <div class="d-flex justify-content-center gap-2">
                                                <button type="button" class="btn btn-action btn-view view-tasks-btn" title="Voir les tâches">
                                                    <i class="bi bi-eye"></i> {# Icône pour "Voir les tâches" #}
                                                </button>
                                                <button type="button" class="btn btn-action btn-delete delete-activity-btn" title="Supprimer">
                                                    <i class="bi bi-trash"></i> {# Icône pour "Supprimer" #}
                                                </button>
                                                <button type="button" class="btn btn-action btn-modify modify-activity-btn" title="Modifier">
                                                    <i class="bi bi-pencil"></i> {# Icône pour "Modifier" #}
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                    <tr data-activity-id="2">
                                        <td>Activité 1</td>
                                        <td>Collaborateur 1</td>
                                        <td>10-08-2025</td>
                                        <td>5</td>
                                        <td>moyenne</td>
                                        <td class="text-center-buttons"> {# Colonne unique pour les boutons d'action #}
                                            <div class="d-flex justify-content-center gap-2">
                                                <button type="button" class="btn btn-action btn-view view-tasks-btn" title="Voir les tâches">
                                                    <i class="bi bi-eye"></i> {# Icône pour "Voir les tâches" #}
                                                </button>
                                                <button type="button" class="btn btn-action btn-delete delete-activity-btn" title="Supprimer">
                                                    <i class="bi bi-trash"></i> {# Icône pour "Supprimer" #}
                                                </button>
                                                <button type="button" class="btn btn-action btn-modify modify-activity-btn" title="Modifier">
                                                    <i class="bi bi-pencil"></i> {# Icône pour "Modifier" #}
                                                </button>
                                            </div>
                                        </td>
                                    </tr>
                                    {# Ajoutez d'autres lignes de données ici si nécessaire #}
                                </tbody>
                             </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        {% block page_content %}{% endblock %}
    </main>

    <!-- Bootstrap JS CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
    {% block javascripts %}{% endblock %}

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            document.querySelectorAll('.view-tasks-btn').forEach(button => {
                button.addEventListener('click', function() {
                    const activityId = this.closest('tr').dataset.activityId;
                    console.log(`Bouton 'Voir les tâches' cliqué pour l'activité ID: ${activityId}`);
                    // Rediriger vers la page des tâches de l'activité
                    // window.location.href = `/activities/${activityId}/tasks`;
                });
            });

            document.querySelectorAll('.delete-activity-btn').forEach(button => {
                button.addEventListener('click', function() {
                    const activityId = this.closest('tr').dataset.activityId;
                    if (confirm(`Êtes-vous sûr de vouloir supprimer l'activité ID: ${activityId} ?`)) {
                        console.log(`Bouton 'Supprimer' cliqué pour l'activité ID: ${activityId}`);
                        // Ici, vous enverriez une requête DELETE au backend Symfony
                        // fetch(`/activities/${activityId}`, { method: 'DELETE' }).then(...);
                    }
                });
            });

            document.querySelectorAll('.modify-activity-btn').forEach(button => {
                button.addEventListener('click', function() {
                    const activityId = this.closest('tr').dataset.activityId;
                    console.log(`Bouton 'Modifier' cliqué pour l'activité ID: ${activityId}`);
                    // Rediriger vers la page de modification de l'activité
                    // window.location.href = `/activities/${activityId}/edit`;
                });
            });
        });
    </script>
</body>
</html>
