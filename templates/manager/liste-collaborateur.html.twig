<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Attribuer une tâche{% endblock %}</title>
    <!-- Bootstrap CSS CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
    <!-- Bootstrap Icons CDN (IMPORTANT pour les icônes du sidebar) -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
    <style>
        /* Styles pour le corps et le padding pour le sidebar */
        body {
            padding-left: 280px; /* Largeur du sidebar */
            background-color: #F5F5F5; /* Couleur de fond gris très doux pour le contenu principal */
        }
        /* Pour la responsivité sur mobile, vous pourriez vouloir cacher le sidebar fixe et utiliser un offcanvas */
        @media (max-width: 767.98px) { /* Moins de 768px (taille md de Bootstrap) */
            body {
                padding-left: 0; /* Pas de padding sur mobile si le sidebar est un offcanvas */
            }
            .sidebar-fixed-desktop { /* Cache le sidebar fixe sur mobile */
                display: none !important;
            }
        }

        /* Styles pour les cartes (si utilisées pour le tableau) */
        .card {
            border-radius: 12px; /* Bords plus arrondis */
            border: 1px solid #e0e0e0; /* Bordure subtile */
            box-shadow: 0 4px 15px rgba(0,0,0,0.08); /* Ombre plus douce et moderne */
        }

        /* STYLES DE TABLEAU MODERNISÉS (type Bootstrap épuré) */
        .table { /* Utilise la classe Bootstrap par défaut */
            border-collapse: collapse; /* Fusionne les bordures des cellules */
            width: 100%;
            margin-bottom: 0;
            overflow: hidden; /* Assure que les coins du tableau sont coupés par le border-radius de la carte */
        }

        .table thead th {
            color: #555; /* Couleur de texte plus douce */
            font-weight: 600;
            padding: 0.8rem 1.2rem; /* Padding réduit pour l'en-tête */
            text-align: left;
            border-bottom: 2px solid #e0e0e0; /* Bordure plus prononcée sous l'en-tête */
            text-transform: uppercase;
            font-size: 0.85rem;
            letter-spacing: 0.05em;
            border-right: 1px solid #e0e0e0; /* Bordure verticale subtile */
        }
        .table thead th:last-child {
            border-right: none; /* Pas de bordure à droite pour la dernière colonne d'en-tête */
        }

        .table tbody td {
            padding: 0.8rem 1.2rem; /* Padding réduit pour les cellules */
            vertical-align: middle;
            border-bottom: 1px solid #eee; /* Bordure très légère entre les lignes */
            border-right: 1px solid #eee; /* Bordure verticale très légère */
            color: #333;
        }
        .table tbody td:last-child {
            border-right: none; /* Pas de bordure à droite pour la dernière colonne de données */
        }

        /* Effet de survol pour les lignes du tableau (fourni par .table-hover de Bootstrap) */
        .table-hover tbody tr:hover {
            background-color: #f8f9fa; /* Fond légèrement grisé au survol */
            cursor: pointer;
        }

        /* Supprime la bordure inférieure de la dernière ligne */
        .table tbody tr:last-child td {
            border-bottom: none;
        }

        /* Centrer le contenu des cellules des boutons */
        .table td.text-center-buttons {
            text-align: center;
            vertical-align: middle;
            width: 1%; /* Rendre la colonne du bouton compacte */
            white-space: nowrap; /* Empêche le texte du bouton de passer à la ligne */
        }

        /* Styles pour le bouton "Attribuer" */
        .btn-assign {
            background-color: #0d6efd; /* Couleur primaire de Bootstrap */
            color: white;
            border-radius: 6px; /* Bords légèrement arrondis */
            padding: 0.6rem 1.2rem;
            font-size: 0.9rem;
            font-weight: 500;
            transition: background-color 0.2s ease, transform 0.1s ease;
        }
        .btn-assign:hover {
            background-color: #0b5ed7; /* Couleur plus foncée au survol */
            transform: translateY(-1px); /* Léger effet de soulèvement */
        }
        .btn-assign:active {
            transform: translateY(0);
        }

        /* Style pour les boutons d'icône */
        .btn-icon {
            padding: 0.5rem 0.75rem; /* Ajuster le padding pour les icônes */
            font-size: 1rem; /* Taille de l'icône */
            line-height: 1; /* Assurer un bon alignement vertical de l'icône */
        }
    </style>
    {% block stylesheets %}{% endblock %}
</head>
<body>
    {# Inclusion directe du Sidebar #}
    <div class="sidebar-fixed-desktop">
        {% include 'components/sidebar.html.twig' %}
    </div>

    <main class="container-fluid py-5">
        {# Le titre peut être dynamique en fonction de la tâche #}
        <h1 class="mb-5 text-center">Liste des collaborateurs à attribuer la tâche : "{{ taskName is defined ? taskName : 'tache 1' }}"</h1>

        <div class="row justify-content-center">
            {# Augmentation de la largeur de la colonne pour la carte du tableau #}
            <div class="col-md-11 col-lg-10"> {# Prend plus de largeur sur les écrans moyens et grands #}
                <div class="card h-100">
                    <div class="card-body p-0">
                        <h5 class="card-title mb-3 px-4 pt-4 text-center">Collaborateurs disponibles</h5>
                        <div class="table-responsive">
                            <table class="table table-hover mb-0"> {# Utilisation des classes Bootstrap par défaut #}
                                <thead>
                                    <tr>
                                        <th>Identifiant</th>
                                        <th>Nom</th>
                                        <th>Charge de travail</th>
                                        <th class="text-center-buttons">Action</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    {# Exemple de collaborateurs. En production, ces données viendraient de votre backend. #}
                                    <tr>
                                        <td>1</td>
                                        <td>Collaborateur 1</td>
                                        <td>8</td>
                                        <td class="text-center-buttons">
                                            <button type="button" class="btn btn-assign btn-icon" data-collaborator-id="1" title="Attribuer">
                                                <i class="bi bi-person-plus"></i>
                                            </button>
                                        </td>
                                    </tr>
                                    <tr>
                                        <td>2</td>
                                        <td>Collaborateur 2</td>
                                        <td>5</td>
                                        <td class="text-center-buttons">
                                            <button type="button" class="btn btn-assign btn-icon" data-collaborator-id="2" title="Attribuer">
                                                <i class="bi bi-person-plus"></i>
                                            </button>
                                        </td>
                                    </tr>
                                    {# Ajoutez d'autres lignes de données ici si nécessaire #}
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        {% block page_content %}{% endblock %}
    </main>

    <!-- Bootstrap JS CDN -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-geWF76RCwLtnZ8qwWowPQNguL3RmwHVBC9FhGdlKrxdiJJigb/j/68SIy3Te4Bkz" crossorigin="anonymous"></script>
    {% block javascripts %}{% endblock %}

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            document.querySelectorAll('.btn-assign').forEach(button => {
                button.addEventListener('click', function() {
                    const collaboratorId = this.dataset.collaboratorId;
                    // Récupérer l'ID de la tâche depuis l'URL ou un élément caché si nécessaire
                    const taskId = new URLSearchParams(window.location.search).get('taskId') || '1'; // Exemple: récupérer de l'URL ou par défaut

                    console.log(`Bouton 'Attribuer' cliqué pour le collaborateur ID: ${collaboratorId} et la tâche ID: ${taskId}`);
                    // Ici, vous intégreriez votre logique pour envoyer la requête d'attribution au backend Symfony.
                    // Par exemple, via Fetch API:
                    /*
                    fetch(`/tasks/${taskId}/assign/${collaboratorId}`, {
                        method: 'POST',
                        headers: {
                            'Content-Type': 'application/json',
                            // 'X-Requested-With': 'XMLHttpRequest' // Si vous utilisez des requêtes AJAX
                        },
                        // body: JSON.stringify({ taskId: taskId, collaboratorId: collaboratorId }) // Si vous envoyez un corps JSON
                    })
                    .then(response => response.json())
                    .then(data => {
                        console.log('Attribution réussie:', data);
                        // Mettre à jour l'UI ou rediriger
                    })
                    .catch(error => {
                        console.error('Erreur d\'attribution:', error);
                    });
                    */
                });
            });
        });
    </script>
</body>
</html>
